# Ceiling Grid Planner

A prototype ceiling grid planner built with React

## Overview
This tool allows users to interact with a scalable ceiling grid, supporting zooming,
panning, and placement of elements.